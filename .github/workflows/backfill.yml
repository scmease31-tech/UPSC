name: Backfill Derived Content

# Derives vocabulary, flashcards, and government schemes from the articles
# already stored in Firestore. Run manually after enabling the generators, or
# any time you want to (re)derive content from the full article history.
# Dedup-safe: existing docs are skipped.

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (no upload)'
        type: boolean
        default: false
      prune_schemes:
        description: 'Delete scheme docs whose names are not real schemes'
        type: boolean
        default: false

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/content-scraper/package-lock.json

      - name: Install dependencies
        working-directory: backend/content-scraper
        run: npm ci || npm install

      - name: Run backfill
        working-directory: backend/content-scraper
        env:
          FIREBASE_SERVICE_ACCOUNT_B64: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_B64 }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" == "true" ]; then
            node backfill.js --dry-run
          else
            node backfill.js
          fi

      # backfill.js can only CREATE docs — uploadToCollection skips ids that
      # already exist. Schemes are keyed on their name alone, so ones stored
      # before generateSchemes produced detailedDescription / keyFeatures /
      # upscRelevance / ministry would keep their thin shape forever and the
      # app's detail sheet would stay near-empty. This fills those fields in.
      - name: Fill scheme detail onto existing docs
        working-directory: backend/content-scraper
        env:
          FIREBASE_SERVICE_ACCOUNT_B64: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_B64 }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" == "true" ]; then
            node enrich-schemes.js --dry-run
          else
            node enrich-schemes.js
          fi

      # Names are pattern-matched out of prose, so earlier, looser runs stored
      # headings ("Conclusion The PM SHRI Scheme"), sentence fragments and
      # organisations that are not schemes. generateSchemes now filters these at
      # write time; this removes the ones already stored. Always audits; only
      # deletes when prune_schemes is set.
      - name: Audit scheme names
        working-directory: backend/content-scraper
        env:
          FIREBASE_SERVICE_ACCOUNT_B64: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_B64 }}
        run: |
          if [ "${{ github.event.inputs.prune_schemes }}" == "true" ]; then
            if [ "${{ github.event.inputs.dry_run }}" == "true" ]; then
              node audit-schemes.js --prune --dry-run
            else
              node audit-schemes.js --prune
            fi
          else
            node audit-schemes.js
          fi
